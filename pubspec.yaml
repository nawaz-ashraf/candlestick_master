name: candlestick_master
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  # State Management
  provider: ^6.1.1
  
  # Navigation
  go_router: ^13.0.0
  
  # Storage
  shared_preferences: ^2.2.2

  # Using sqflite or hive is better for offline, using shared_preferences for simple settings
  sqflite: ^2.3.0
  path: ^1.8.3

  # UI/Assets
  google_fonts: ^6.1.0
  flutter_markdown: ^0.6.14
  
  # JSON
  json_annotation: ^4.8.1

  # Charts
  candlesticks: any # Popular package
  
  # Firebase
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.14.0
  firebase_messaging: ^14.6.1
  firebase_analytics: ^10.7.4

  # Ads (Monetization)
  google_mobile_ads: ^5.1.0

  # Sharing (Deep Links / Marketing)
  share_plus: ^7.2.1
  path_provider: ^2.1.1
  
  # Notifications
  flutter_local_notifications: ^18.0.1
  timezone: ^0.9.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.7
  json_serializable: ^6.7.1


flutter:
  uses-material-design: true
  assets:
    - assets/patterns.json
    - assets/quiz_questions.json
    - assets/images/patterns/
    - assets/AppIcons/
